name: flutter_easy_treeview
description: This widget visualises a tree structure, where a node can be any widget.
version: 1.0.5
homepage: https://github.com/nslogx/flutter_easy_treeview

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  quiver:
  collection:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
