name: flutter_easy_treeview
description: This widget visualises a tree structure, where a node can be any widget.
version: 0.2.0
homepage: https://github.com/kokohuang/flutter_easy_treeview

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  quiver: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
